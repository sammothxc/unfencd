# Copy to .env and fill in values. Never commit .env.

POSTGRES_DB=unfencd
POSTGRES_USER=unfencd
POSTGRES_PASSWORD=changeme

# Frontend — override if using a reverse proxy or Cloudflare tunnel
VITE_MARTIN_URL=http://localhost:3000
VITE_TILESERVER_URL=http://localhost:8080
